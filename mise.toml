[tools]
python = "3.11.14"

[env]
_.python.venv = { path = ".venv", create = true }
PYTHONPATH = "application/base/src"

[tasks.test]
run = "pytest -c application/base/pyproject.toml tests"

[tasks.lint]
run = "ruff check --config application/base/pyproject.toml ."
